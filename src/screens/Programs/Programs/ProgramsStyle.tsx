import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#fff",
    },
    mainView: {
        padding: 20,
    },
    noDataFound: {
        marginTop: "50%",
        width: "100%",
        justifyContent: "center",
        alignItems: "center",
    }
});
