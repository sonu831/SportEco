import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CreatePlayer = () => {
    return (
        <View>
            <Text>CreatePlayer</Text>
        </View>
    )
}

export default CreatePlayer

const styles = StyleSheet.create({})